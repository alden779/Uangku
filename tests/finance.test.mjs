import {test} from 'node:test';import assert from 'node:assert/strict';import {totals,validate,validateState,goalBalance} from '../finance.js';
const tx=(id,type,amount,goal='')=>({id,type,amount,goal,date:'2026-09-16',category:'Lainnya',note:''});
test('Transfers preserve total wealth and do not inflate income or expenses',()=>{const s={budget:1000,goals:[{id:'g',name:'Darurat',target:1000}],transactions:[tx('1','income',1000),tx('2','expense',100),tx('3','save',300,'g'),tx('4','withdraw',50,'g')]};assert.deepEqual(totals(s,'2026-09'),{income:1000,expense:100,savings:250,balance:650});assert.equal(goalBalance(s,'g'),250);assert.equal(totals(s,'2026-08').expense,0)});
test('Invalid withdrawals and malformed imports are rejected',()=>{const s={budget:0,goals:[{id:'g',name:'Darurat',target:1000}],transactions:[tx('1','save',100,'g')]};assert.throws(()=>validate(s,tx('2','withdraw',101,'g')));assert.throws(()=>validate(s,tx('2','expense',0)));assert.throws(()=>validateState({...s,transactions:[tx('2','withdraw',101,'g')]}));assert.throws(()=>validateState({...s,transactions:[tx('1','expense',20),tx('1','expense',30)]}));assert.throws(()=>validateState({...s,transactions:[tx('1','save',20,'missing')]}))});
